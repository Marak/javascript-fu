// returns a single random number based on a range 
exports.formatRandom = function(range) {
		r = Math.floor(Math.random()*range);
		return r;
};

exports.formatShuffle = function( numbery ){
  numbery = fu.toNumber( numerby );
  // apply formatting
  return numbery;
}


exports.formatReverse = function( numbery ){
  numbery = fu.toNumber( numerby );
  // apply formatting
  return numbery;
}

exports.formatNumber = function( numbery ){
  numbery = fu.toNumber( numerby );
  // apply formatting
  return numbery;
}